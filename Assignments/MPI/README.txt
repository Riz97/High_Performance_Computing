Parallelise the above program that computes the pi value.

Determine the scalability on a single workstation and using all the compute capabilities of the cluster.

Send a zip file containing both the code and the report.

